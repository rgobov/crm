server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health

spring:
  datasource:
    url: ${DATABASE_URL}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

logging:
  level:
    com.tryneuro: INFO
