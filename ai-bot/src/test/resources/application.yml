spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jdbc:
    template:
      max-rows: 1000
internal:
  api:
    secret: test-secret
server:
  port: 0
