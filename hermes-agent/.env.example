# Hermes Agent configuration
# Copy to .env and fill in values

# Telegram bot token (from @BotFather)
TELEGRAM_BOT_TOKEN=

# (removed: per-tenant keys are stored in ai-knowledge-service SQLite, set via CRM AI settings page)

# Internal auth secret (must match CRM stack)
INTERNAL_SECRET=try-neuro-internal-secret-2026

# CRM backend URL (Docker internal)
CRM_BACKEND_URL=http://backend:8080

# AI Knowledge Service URL (Docker internal)
AI_KNOWLEDGE_URL=http://ai-knowledge-service:8082
